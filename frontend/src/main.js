import Vue from 'vue';
import App from './App.vue';

// axios.defaults.baseURL = 'https://localhost:8000';
// vue.config.productionTip = false;

new Vue({
    render: (h) => h(App),
}).$mount('#app');
